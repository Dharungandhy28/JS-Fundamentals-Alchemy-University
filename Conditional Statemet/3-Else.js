// Else

/* if a is not equal to b then return true
otherwise return false 
*/

function isNotEqual(a, b) {
  if (a !== b) {
    return true;
  } else {
    return false;
  }
}

console.log(isNotEqual(1, 45));
