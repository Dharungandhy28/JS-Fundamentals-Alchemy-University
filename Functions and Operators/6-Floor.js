// Floor

/* Take the argument x and use Math.floor
to turn it into an integer without the values
after the decimal place once you have this floored
value, return it 
*/

function getFloor(x) {
  return Math.floor(x);
}

console.log(getFloor(88.854582));
